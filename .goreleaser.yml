version: 2

builds:
  - id: appetite
    main: ./cmd/appetite
    binary: appetite
    flags:
      - -trimpath
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w
      - -X github.com/luuuc/appetite/internal/version.Version={{.Version}}
    goos:
      - darwin
      - linux
    goarch:
      - amd64
      - arm64

archives:
  - id: appetite
    ids: [appetite]
    formats: [tar.gz]
    name_template: "appetite_{{ .Version }}_{{ .Os }}_{{ .Arch }}"

checksum:
  name_template: "appetite_{{ .Version }}_checksums.txt"

changelog:
  use: github
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^chore:"
      - "^ci:"
      - "^test:"
      - "^refactor:"

release:
  github:
    owner: luuuc
    name: appetite
  prerelease: auto
  make_latest: true
